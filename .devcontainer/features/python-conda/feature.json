{
  "name": "python-conda",
  "version": "0.1.0",
  "maintainer": {"name":"Emmanuel BRUNO","email":"emmanuel.bruno@univ-tln.fr"},
  "id": "python-conda",
  "description": "Installs Miniforge and updates environment from Artefacts/environment.yml",
  "dependsOn": ["user"],
  "options": {
    "miniforge_channel": {
      "type": "string",
      "default": "conda-forge",
      "description": "Channel used by conda"
    }
  },
  "postInstallCheck": {
    "command": "conda --version && python --version",
    "description": "Verify conda and python are available"
  }
}
