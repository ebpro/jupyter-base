{
  "id": "java-devtools",
  "name": "Java Development Stack (meta-feature)",
  "version": "2.0.0",
  "description": "Meta-feature: Installs complete Java development stack (JDK + Maven + Gradle). For backward compatibility, use granular features (java-jdk, java-maven, java-gradle) for better caching.",
  "dependsOn": ["java-jdk", "java-maven", "java-gradle"],
  "options": {
    "JDK_VERSION": {"type": "string", "default": "25", "description": "JDK version to install (passed to java-jdk feature)"},
    "SDKMAN_JAVA_IDENTIFIER": {"type": "string", "default": "temurin", "description": "JDK distribution (passed to java-jdk feature)"}
  },
  "provides": [],
  "postInstallCheck": {
    "command": "test -d /home/jovyan/.sdkman && sdk version && test -d /home/jovyan/.sdkman/candidates/java/current",
    "description": "Verify SDKMAN is installed and Java is available"
  },
  "platforms": ["linux/amd64","linux/arm64"],
  "maintainer": {"name":"Emmanuel BRUNO","email":"emmanuel.bruno@univ-tln.fr"}
}
