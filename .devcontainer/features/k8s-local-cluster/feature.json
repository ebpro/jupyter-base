{
  "name": "k8s-local-cluster",
  "version": "0.1.0",
  "maintainer": {"name":"Emmanuel BRUNO","email":"emmanuel.bruno@univ-tln.fr"},
  "id": "k8s-local-cluster",
  "description": "Local Kubernetes cluster using k3d for CI/CD teaching and testing. Requires docker-dind.",
  "dependsOn": ["docker-dind", "kubernetes-client", "_lib/download-release"],
  "provides": ["k3d", "k8s-cluster"],
  "platforms": ["linux/amd64","linux/arm64"],
  "options": {
    "autoCreateCluster": {
      "type": "boolean",
      "default": false,
      "description": "Automatically create a default k3d cluster on startup"
    },
    "clusterName": {
      "type": "string",
      "default": "devcluster",
      "description": "Name for the default cluster (if autoCreateCluster is true)"
    },
    "agents": {
      "type": "string",
      "default": "2",
      "description": "Number of agent nodes in default cluster"
    }
  },
  "postInstallCheck": {
    "command": "k3d version",
    "description": "Verify k3d is available"
  },
  "vscode": {
    "extensions": [
      "ms-kubernetes-tools.vscode-kubernetes-tools"
    ]
  }
}
