# TypeScript

**Installs:** -g, @types/node, else, install, npm, ...

**Purpose:** Installs TypeScript compiler and type definitions globally via npm

## Description

Installs TypeScript compiler and type definitions globally via npm


## Dependencies

- `node`
