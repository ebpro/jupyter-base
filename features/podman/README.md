# podman

**Installs:** podman

**Purpose:** Installs Podman client (user-space container runtime) via apt when available.

## Description

Installs Podman client (user-space container runtime) via apt when available.


## Dependencies

- `container-user`

## Usage
Describe how to enable or configure this feature.

## Options
List feature-specific options and defaults.
