{
  "id": "xeus-sql",
  "name": "Xeus-SQL Jupyter Kernel",
  "version": "1.0.0",
  "description": "Native SQL kernel for Jupyter with database connectivity (SQLAlchemy, PostgreSQL, MySQL)",
  "options": {},
  "customizations": {
    "solen": {
      "category": "database",
      "tags": [
        "jupyter",
        "sql",
        "kernel",
        "postgresql",
        "mysql"
      ],
      "complexity": "simple",
      "installTimeMinutes": 2,
      "requiresRestart": false
    }
  },
  "dependsOn": [
    "python-base"
  ],
  "postInstallCheck": {
    "command": "sh -c 'command -v jupyter >/dev/null 2>&1 && jupyter --version || echo \"jupyter not found\"'",
    "description": "Verify Jupyter is available (kernel registration can be checked separately)"
  },
  "documentationURL": "./features/xeus-sql/README.md"
}
