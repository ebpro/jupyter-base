# Java SDK Manager (SDKMAN)

**Purpose:** Installs SDKMAN so SDK-managed Java tools (maven, gradle, jdks) can be provisioned.

## Description

Installs SDKMAN so SDK-managed Java tools (maven, gradle, jdks) can be provisioned.


## Dependencies

- `container-user`
