# Docker Buildx

**Installs:** docker-buildx-plugin

**Purpose:** Install Docker Buildx plugin for advanced build capabilities and multi-platform builds

## Description

Install Docker Buildx plugin for advanced build capabilities and multi-platform builds


## Dependencies

- `docker-cli`
