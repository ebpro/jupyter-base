{
  "id": "docker",
  "name": "docker (legacy stub)",
  "version": "0.0.0",
  "note": "This stub was created temporarily. The canonical check is now in features/docker-cli/check_version.py. Consider removing this file.",
  "deprecated": true,
  "postInstallCheck": {
    "command": "docker --version",
    "description": "Verify Docker CLI is available"
  },
  "description": "Metadata placeholder for Docker Engine configuration; prefer using the `docker-cli` feature for CLI installs.",
  "documentationURL": "./features/docker/README.md"
}
