# jupyter-base

**Purpose:** Installs core Jupyter packages (jupyter, jupyterlab, nb_conda_kernels) into conda base

## Description

Installs core Jupyter packages (jupyter, jupyterlab, nb_conda_kernels) into conda base


## Dependencies

- `python-conda`
