# java-kernel

**Purpose:** Installs a Java Jupyter kernel (IJava) from release or submodule and registers kernelspec.

## Description

Installs a Java Jupyter kernel (IJava) from release or submodule and registers kernelspec.


## Dependencies

- `java-jdk`

- `jupyter-kernels`

## Usage
Describe how to enable or configure this feature.

## Options
List feature-specific options and defaults.
